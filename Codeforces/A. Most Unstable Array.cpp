#include<btits/stdc++.h>
using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);

    int test;
    cin>>test;

    while(test--)
    {
        int a,b;
        cin>>a>>b;

        if(a == 1)
            cout<<"0"<<endl;
        else if(a <= 4)
            cout<<a<<endl;
        else if()
    }

    return 0;
}
