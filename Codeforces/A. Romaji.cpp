#include<bits/stdc++.h>
using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    char a[100010];

    cin.getline(a,100010);

    int l,f = 0,ff = 0;

    l = strlen(a);

    for(int i=1;i<l;i++)
    {

    }

    if(f == 2 && ff == 2)
    {
        cout<<"YES"<<endl;
    }
    else
    {
        cout<<"NO"<<endl;
    }

    return 0;
}

