#include<bits/stdc++.h>
using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int l;
    char a,c[105];

    cin>>a;
    cin>>c;

    l = strlen(c);

    for(int i=0;i<l;i++)
    {

    }

    return 0;
}
