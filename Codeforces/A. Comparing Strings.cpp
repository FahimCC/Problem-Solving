#include<bits/stdc++.h>
using namespace std;

int main()
{
    ios_base:sync_with_stdio(false);
    cin.tie(NULL);

    char a[100010],b[100010];

    cin.getline(a,100010);
    cin.getline(b,100010);

    int la,lb;

    la = strlen(a);
    lb = strlen(b);

    for(int i=0;i<la;i++)
    {

    }

    return 0;
}
