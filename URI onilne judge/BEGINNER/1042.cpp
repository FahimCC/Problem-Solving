#include<stdio.h>
int main()
{
    int a,b,c,x,x1,x2,x3;
    scanf("%d%d%d",&a,&b,&c);
    {

    if(x<0 && (x%2)==0)
        printf("%d\n",x);
    if(x<0 && (x%2)!=0)
        printf("%d\n",x1);
    if(x>0 && (x%2)!=0)
        printf("%d\n",x2);
    if(x>0 && (x%2)==0)
        printf("%d\n",x3);
    }

        printf("\n\n%d\n%d\n%d\n",a,b,c);
    return 0;
}
